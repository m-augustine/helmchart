{{- range $name, $item := $.Values.kuma.resources.meshes }}
apiVersion: kuma.io/v1alpha1
kind: Mesh
metadata: 
  name: {{ $item.name }}
spec: 
  {{- if $item.mtls.enabled }}
  mtls: 
    enabled: {{ $item.mtls.enabled }}
    enabledBackend: {{ $item.mtls.enabledBackend }}
    {{- with $item.mtls.backends }}
    backends:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- if $item.tracing.enabled }}
  tracing: 
    defaultBackend: {{ $item.tracing.defaultBackend }}
    {{- with $item.tracing.backends }}
    backends:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- if $item.logging.enabled }}
  logging: 
    defaultBackend: {{ $item.logging.defaultBackend }}
    {{- with $item.logging.backends }}
    backends:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
  {{- if $item.metrics.enabled }}
  metrics: 
    enabledBackend: {{ $item.metrics.enabledBackend }}
    {{- with $item.metrics.backends }}
    backends:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- end }}
---
{{- end }}