apiVersion: v1
kind: Service
metadata:
  name: dashboard
  # annotations:
spec:
  type: ClusterIP
  selector:
    app: wazuh-dashboard
  ports:
    - name: dashboard
      port: 443
      targetPort: 5601